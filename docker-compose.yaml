version: "3.9"

services:
  api:
    container_name: api
    build: .
    command: uvicorn src.main:app --reload --host 0.0.0.0 --port 8000
    ports:
      - '8000:8000'
    volumes:
      - .:/code
    networks:
      - backend


networks:
  backend:
    driver: bridge